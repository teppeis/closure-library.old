declare module goog.ui {

    /**
     * The renderer for {@link goog.ui.DatePicker}. Renders the date picker's
     * navigation header and footer.
     * @interface
     */
    export function DatePickerRenderer(): void;
}
